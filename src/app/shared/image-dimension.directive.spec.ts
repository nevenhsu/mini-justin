import { ImageDimensionDirective } from './image-dimension.directive';

describe('ImageDimensionDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageDimensionDirective();
    expect(directive).toBeTruthy();
  });
});
